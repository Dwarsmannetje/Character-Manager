import { CalculatedVariable } from './calculated-variable';

describe('CalculatedVariable', () => {
  it('should create an instance', () => {
    expect(new CalculatedVariable()).toBeTruthy();
  });
});
